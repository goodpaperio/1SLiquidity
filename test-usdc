#!/bin/bash

# Test USDC pairs with chunking
# Usage: ./test-usdc [chunk-size] [output-file]

CHUNK_SIZE=${1:-10}
OUTPUT_FILE=${2:-"test-results-usdc-$(date +%Y%m%d_%H%M%S).json"}

echo "🧪 Running USDC trade tests..."
echo "📦 Chunk size: $CHUNK_SIZE tokens"
echo "📁 Output: $OUTPUT_FILE"
echo ""

# Get pair count
USDC_COUNT=$(jq '.totalCount' config/usdc_pairs_clean.json)
NUM_CHUNKS=$(( (USDC_COUNT + CHUNK_SIZE - 1) / CHUNK_SIZE ))

echo "📊 USDC: $USDC_COUNT pairs → $NUM_CHUNKS chunks"
echo ""

# Temporary file for logs
TEMP_LOG=$(mktemp)

# Run chunks
for ((chunk=0; chunk<NUM_CHUNKS; chunk++)); do
    start=$((chunk * CHUNK_SIZE))
    end=$((start + CHUNK_SIZE))
    if [ $end -gt $USDC_COUNT ]; then
        end=$USDC_COUNT
    fi
    
    echo "   Chunk $((chunk+1))/$NUM_CHUNKS: tokens $start-$end"
    START_INDEX=$start END_INDEX=$end forge test --match-test "test_PlaceTradeWithUSDCTokens" -vv 2>&1 | tee -a "$TEMP_LOG"
done

echo ""
echo "📊 Extracting results..."
bash ./scripts/extract-events-to-json.sh "$OUTPUT_FILE" < "$TEMP_LOG"

# Cleanup
rm -f "$TEMP_LOG"

echo "✅ USDC tests completed!"