# RPC (required for live phases)
MAINNET_RPC_URL=https://eth-mainnet.g.alchemy.com/v2/YOUR_KEY

# Repo root (defaults to parent of liquidity-bot)
# REPO_ROOT=..

# Bot keys — one per bot id (BOT_<ID>_KEY), set after `npm run generate bot -- <id>`
# BOT_ALPHA_KEY=0x...

# Price hints for nominal USD sizing (optional; defaults used if unset)
ETH_USD=3500
BTC_USD=95000

# Safety: 1 = log only, no transactions (default for dev)
DRY_RUN=1

# Deploy target (phase B+)
# DEPLOY_HOST=ubuntu@your-ec2.amazonaws.com
# DEPLOY_PATH=/opt/1sliquidity
