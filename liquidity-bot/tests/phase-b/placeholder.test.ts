import { describe, it } from 'vitest';

describe('phase B — ops (generate / deploy / start)', () => {
  it.todo('generate bot creates wallet + bots/<id>.json + env key');
  it.todo('deploy bot syncs to AWS');
  it.todo('start bot launches PM2 process');
});
