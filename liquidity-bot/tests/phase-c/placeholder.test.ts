import { describe, it } from 'vitest';

describe('phase C — scanner', () => {
  it.todo('detects spread >= minSpreadBps with liquidity ordering');
  it.todo('scan:dry-run logs opportunities without txs');
});
